{
    "configurations": [
        {
            "name": "Launch Chrome",
            "request": "launch",
            "type": "chrome",
            "url": "http://localhost:8080",
            "webRoot": "${workspaceFolder}"
        },
        {
            "command": "bundle exec jekyll serve --livereload",
            "name": "Start server",
            "type": ""
        }
    ]
}